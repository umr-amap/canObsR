# Create the recommended files architecture

Create the recommended files architecture.

## Usage

``` r
create_files_architecture(main_folder_path = getwd(), main_folder_name = NULL)
```

## Arguments

- main_folder_path:

  Character. The path where to create the main folder

- main_folder_name:

  Character. The name of the main folder. Must be provided.
